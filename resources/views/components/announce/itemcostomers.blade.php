<div class="item costomers">
    <!-- Icon -->
    <div class="icon"><span class="material-symbols-outlined">{{ $icon }}</span></div>

    <!-- Right side content -->
    <div class="right">
        <div class="info">
            <h3>{{ $title }}</h3>
            <small class="text-muted">{{ $subtitle }}</small>
        </div>
        <h5 class="success">{{ $percentage }}</h5>
        <h3>{{ $count }}</h3>
    </div>
</div>
